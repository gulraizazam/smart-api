# Credentials for scripts/deploy-portal.py — copy to scripts/deploy-backend.env
# (git-ignored) and fill in. Never commit the real values.
#
# Targets PROD, NOT demo:
#   portal.alluraesthetics.pk  →  /home/u941750079/domains/alluraesthetics.pk/public_html/portal
SSH_HOST=45.13.255.150
SSH_PORT=65002
SSH_USER=u941750079
SSH_PASS=REPLACE_ME
REMOTE_ROOT=/home/u941750079/domains/alluraesthetics.pk/public_html/portal

# GitHub PAT with `contents:read` on gulraizazam/smart-api (fine-grained recommended).
# Persisted into the server's origin URL on first pull; rotate here + re-run
# deploy-portal.py to refresh. Never commit the real value.
GH_TOKEN=REPLACE_ME
